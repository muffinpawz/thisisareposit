
<h1>Muffin's anime quote and random stuff blog!</h1>

<h3>Greetings <%= current_user.first_name if current_user %></h3>

<p>______________________________________________________</p>

<%= link_to "Create New Portfolio", new_portfolio_url  if logged_in?(:site_admin) %>

<p>-Sort Portfolios-</p>
<a href="portfolios/ylia">About Your Lie in April</a>
<a href="portfolios/fmab">About Full Metal Alchemist Brotherhood</a>
<p>______________________________________________________</p>

<% @portfolio_items.each do |portfolio_item| %>
<p><%= link_to portfolio_item.title,
portfolio_show_path(portfolio_item) %></p>
<p><%=portfolio_item.subtitle%></p>
<p><%=portfolio_item.body%></p>
<%= image_tag portfolio_item.thumb_image unless 
portfolio_item.thumb_image.nil? %>
<%= link_to 'Edit', edit_portfolio_path(portfolio_item)  if logged_in?(:site_admin) %>
<%= link_to 'Delete', portfolio_path(portfolio_item), method: :delete, data: { confirm: 'ARE YOU 100% CERTAIN!!?!?' }  if logged_in?(:site_admin)%>
<% end %>
